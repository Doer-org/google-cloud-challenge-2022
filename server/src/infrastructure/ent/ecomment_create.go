// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Doer-org/google-cloud-challenge-2022/infrastructure/ent/ecomment"
	"github.com/Doer-org/google-cloud-challenge-2022/infrastructure/ent/event"
	"github.com/Doer-org/google-cloud-challenge-2022/infrastructure/ent/user"
	"github.com/google/uuid"
)

// EcommentCreate is the builder for creating a Ecomment entity.
type EcommentCreate struct {
	config
	mutation *EcommentMutation
	hooks    []Hook
}

// SetBody sets the "body" field.
func (ec *EcommentCreate) SetBody(s string) *EcommentCreate {
	ec.mutation.SetBody(s)
	return ec
}

// SetID sets the "id" field.
func (ec *EcommentCreate) SetID(u uuid.UUID) *EcommentCreate {
	ec.mutation.SetID(u)
	return ec
}

// SetNillableID sets the "id" field if the given value is not nil.
func (ec *EcommentCreate) SetNillableID(u *uuid.UUID) *EcommentCreate {
	if u != nil {
		ec.SetID(*u)
	}
	return ec
}

// SetEventID sets the "event" edge to the Event entity by ID.
func (ec *EcommentCreate) SetEventID(id uuid.UUID) *EcommentCreate {
	ec.mutation.SetEventID(id)
	return ec
}

// SetNillableEventID sets the "event" edge to the Event entity by ID if the given value is not nil.
func (ec *EcommentCreate) SetNillableEventID(id *uuid.UUID) *EcommentCreate {
	if id != nil {
		ec = ec.SetEventID(*id)
	}
	return ec
}

// SetEvent sets the "event" edge to the Event entity.
func (ec *EcommentCreate) SetEvent(e *Event) *EcommentCreate {
	return ec.SetEventID(e.ID)
}

// SetUserID sets the "user" edge to the User entity by ID.
func (ec *EcommentCreate) SetUserID(id uuid.UUID) *EcommentCreate {
	ec.mutation.SetUserID(id)
	return ec
}

// SetNillableUserID sets the "user" edge to the User entity by ID if the given value is not nil.
func (ec *EcommentCreate) SetNillableUserID(id *uuid.UUID) *EcommentCreate {
	if id != nil {
		ec = ec.SetUserID(*id)
	}
	return ec
}

// SetUser sets the "user" edge to the User entity.
func (ec *EcommentCreate) SetUser(u *User) *EcommentCreate {
	return ec.SetUserID(u.ID)
}

// Mutation returns the EcommentMutation object of the builder.
func (ec *EcommentCreate) Mutation() *EcommentMutation {
	return ec.mutation
}

// Save creates the Ecomment in the database.
func (ec *EcommentCreate) Save(ctx context.Context) (*Ecomment, error) {
	var (
		err  error
		node *Ecomment
	)
	ec.defaults()
	if len(ec.hooks) == 0 {
		if err = ec.check(); err != nil {
			return nil, err
		}
		node, err = ec.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*EcommentMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ec.check(); err != nil {
				return nil, err
			}
			ec.mutation = mutation
			if node, err = ec.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(ec.hooks) - 1; i >= 0; i-- {
			if ec.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = ec.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, ec.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*Ecomment)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from EcommentMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ec *EcommentCreate) SaveX(ctx context.Context) *Ecomment {
	v, err := ec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ec *EcommentCreate) Exec(ctx context.Context) error {
	_, err := ec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ec *EcommentCreate) ExecX(ctx context.Context) {
	if err := ec.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (ec *EcommentCreate) defaults() {
	if _, ok := ec.mutation.ID(); !ok {
		v := ecomment.DefaultID()
		ec.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ec *EcommentCreate) check() error {
	if _, ok := ec.mutation.Body(); !ok {
		return &ValidationError{Name: "body", err: errors.New(`ent: missing required field "Ecomment.body"`)}
	}
	if v, ok := ec.mutation.Body(); ok {
		if err := ecomment.BodyValidator(v); err != nil {
			return &ValidationError{Name: "body", err: fmt.Errorf(`ent: validator failed for field "Ecomment.body": %w`, err)}
		}
	}
	return nil
}

func (ec *EcommentCreate) sqlSave(ctx context.Context) (*Ecomment, error) {
	_node, _spec := ec.createSpec()
	if err := sqlgraph.CreateNode(ctx, ec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	return _node, nil
}

func (ec *EcommentCreate) createSpec() (*Ecomment, *sqlgraph.CreateSpec) {
	var (
		_node = &Ecomment{config: ec.config}
		_spec = &sqlgraph.CreateSpec{
			Table: ecomment.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: ecomment.FieldID,
			},
		}
	)
	if id, ok := ec.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := ec.mutation.Body(); ok {
		_spec.SetField(ecomment.FieldBody, field.TypeString, value)
		_node.Body = value
	}
	if nodes := ec.mutation.EventIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   ecomment.EventTable,
			Columns: []string{ecomment.EventColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeUUID,
					Column: event.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ecomment_event = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := ec.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   ecomment.UserTable,
			Columns: []string{ecomment.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeUUID,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ecomment_user = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// EcommentCreateBulk is the builder for creating many Ecomment entities in bulk.
type EcommentCreateBulk struct {
	config
	builders []*EcommentCreate
}

// Save creates the Ecomment entities in the database.
func (ecb *EcommentCreateBulk) Save(ctx context.Context) ([]*Ecomment, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ecb.builders))
	nodes := make([]*Ecomment, len(ecb.builders))
	mutators := make([]Mutator, len(ecb.builders))
	for i := range ecb.builders {
		func(i int, root context.Context) {
			builder := ecb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EcommentMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ecb *EcommentCreateBulk) SaveX(ctx context.Context) []*Ecomment {
	v, err := ecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ecb *EcommentCreateBulk) Exec(ctx context.Context) error {
	_, err := ecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ecb *EcommentCreateBulk) ExecX(ctx context.Context) {
	if err := ecb.Exec(ctx); err != nil {
		panic(err)
	}
}
